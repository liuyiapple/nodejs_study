const fs = require('fs')
// const files = fs.readdirSync(__dirname + '/思考')

// files.forEach((item, index) => {
//   let data = item.split('-')
//   if (data.length == 0) {
//     console.log('找到了')
//   }
//   console.log(data)
// })

// fs.mkdir('./node笔记/2.path.md', (err) => {
//   if (err) return
//   console.log('创建成功')
// })
